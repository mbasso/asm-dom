import expect from 'expect';
import asmDom from '../src/';

describe('VNode', () => {
  const vdom = asmDom();

  it('should create a vnode with a selector', () => {
    // console.log(vdom);
  });
});